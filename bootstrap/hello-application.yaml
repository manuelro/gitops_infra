apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hello-local
  namespace: argocd
spec:
  project: default
  source:
    repoURL: http://host.docker.internal:3000/gitops/infra.git
    targetRevision: HEAD
    path: apps/hello/overlays/local
  destination:
    server: https://kubernetes.default.svc
    namespace: demo
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
